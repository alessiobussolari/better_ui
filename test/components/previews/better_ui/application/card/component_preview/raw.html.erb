<%= render BetterUi::Application::Card::Component.new(
  title: title,
  value: value,
  trend: trend,
  change: change,
  color: color,
  theme: theme,
  size: size,
  rounded: rounded,
  shadow: shadow,
  classes: classes
) %>
