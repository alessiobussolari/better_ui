<%= render(BetterUi::Application::MainComponent.new(
  padding: padding,
  inner_padding: inner_padding,
  rounded: rounded.to_sym,
  shadow: shadow.to_sym,
  with_sidebar: with_sidebar,
  with_navbar: with_navbar
)) do %>
  <div>
    <h1 class="text-2xl font-bold mb-4">Contenuto principale</h1>
    <p class="mb-2">Questo è il contenuto principale della pagina. Il componente Main fornisce una struttura base per tutte le pagine dell'applicazione.</p>
    <p class="mb-2">Il componente configura due livelli di contenitori:</p>
    <ul class="list-disc pl-5 mb-4">
    <% 100.times do %>
      <li class="mb-1">Esterno: gestisce il padding, lo spazio per sidebar e navbar</li>
      <li class="mb-1">Interno: gestisce padding interno, bordi arrotondati e ombra</li>
    <% end %>
    </ul>
  </div>
<% end %> 