<!-- Badge Component istanziato direttamente -->
<%= render BetterUi::General::Badge::Component.new(
  label: label,
  theme: theme,
  size: size,
  shape: shape,
  style: style,
  variant: variant,
  icon: icon,
  icon_position: icon_position
) %>
