<%= bui_badge(
    label: label,
    theme: theme,
    size: size,
    shape: shape,
    style: style,
    orientation: orientation,
    variant: variant,
    icon: icon,
    icon_position: icon_position
) %>