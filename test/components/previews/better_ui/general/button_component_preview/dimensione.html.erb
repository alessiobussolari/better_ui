<% variants.each do |variant| %>
    <%= better_ui_general_button(
        variant[:label],
        type: :default,
        size: variant[:size],
        rounded: :small,
        full_width: false,
        disabled: false) %>
<% end %> 