<% variants.each do |variant| %>
    <%= better_ui_general_button(
        variant[:label],
        type: :default,
        size: :medium,
        rounded: :small,
        full_width: false,
        disabled: variant[:disabled]) %>
<% end %> 