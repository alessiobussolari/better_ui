<%= form_with(url: "/example", method: :post, html: { class: "w-full" }) do |form| %>
  <%= render(BetterUi::General::Form::TimepickerComponent.new(
    name: "orario_preciso",
    label: "Orario (incrementi di 5 minuti)",
    form: form,
    step: 300
  )) %>
<% end %> 