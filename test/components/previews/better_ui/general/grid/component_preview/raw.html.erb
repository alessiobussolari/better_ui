<% cols = { sm: cols_sm, md: cols_md, lg: cols_lg } %>
<% gap = { sm: gap_sm, md: gap_md, lg: gap_lg } %>

<% if with_items %>
  <%= render BetterUi::General::Grid::Component.new(
    cols: cols,
    gap: gap,
    rows: nil,
    started: nil,
    ended: nil,
    row_started: nil,
    row_ended: nil,
    theme: theme,
    padding: padding,
    rounded: rounded,
    justify: justify,
    align: align
  ) do |grid| %>
    <% grid.with_item(span: { lg: 2 }) do %>
      <div class="bg-blue-100 border border-blue-200 p-4 rounded text-center text-blue-800">
        Featured Item (2 cols)
      </div>
    <% end %>
    
    <% grid.with_item do %>
      <div class="bg-gray-100 border border-gray-200 p-4 rounded text-center">
        Item 1
      </div>
    <% end %>
    
    <% grid.with_item do %>
      <div class="bg-gray-100 border border-gray-200 p-4 rounded text-center">
        Item 2
      </div>
    <% end %>
    
    <% grid.with_item(started: { lg: 2 }, ended: { lg: 4 }) do %>
      <div class="bg-green-100 border border-green-200 p-4 rounded text-center text-green-800">
        Positioned Item
      </div>
    <% end %>
    
    <% grid.with_item do %>
      <div class="bg-gray-100 border border-gray-200 p-4 rounded text-center">
        Item 3
      </div>
    <% end %>
    
    <% grid.with_item do %>
      <div class="bg-gray-100 border border-gray-200 p-4 rounded text-center">
        Item 4
      </div>
    <% end %>
  <% end %>
<% else %>
  <%= render BetterUi::General::Grid::Component.new(
    cols: cols,
    gap: gap,
    rows: nil,
    started: nil,
    ended: nil,
    row_started: nil,
    row_ended: nil,
    theme: theme,
    padding: padding,
    rounded: rounded,
    justify: justify,
    align: align
  ) do %>
    <div class="bg-blue-100 border border-blue-200 p-4 rounded text-center">Grid Item 1</div>
    <div class="bg-green-100 border border-green-200 p-4 rounded text-center">Grid Item 2</div>
    <div class="bg-yellow-100 border border-yellow-200 p-4 rounded text-center">Grid Item 3</div>
    <div class="bg-purple-100 border border-purple-200 p-4 rounded text-center">Grid Item 4</div>
    <div class="bg-red-100 border border-red-200 p-4 rounded text-center">Grid Item 5</div>
    <div class="bg-indigo-100 border border-indigo-200 p-4 rounded text-center">Grid Item 6</div>
  <% end %>
<% end %>
