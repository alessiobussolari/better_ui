<%= render BetterUi::General::HeadingComponent.new(
  level: level,
  theme: theme,
  align: align,
  size: size,
  style: style,
  icon: icon,
  subtitle: subtitle,
  with_divider: with_divider
).with_content(text) %>