<%= bui_input_datetime(
  name: name,
  type: type,
  value: value,
  required: required,
  disabled: disabled,
  min: min,
  max: max,
  theme: theme,
  size: size,
  rounded: rounded,
  classes: classes
) %>