<%= render BetterUi::General::Input::Pin::Component.new(
  name: name,
  value: value,
  length: length,
  placeholder: placeholder,
  required: required,
  disabled: disabled,
  theme: theme,
  size: size,
  classes: classes
) %>
