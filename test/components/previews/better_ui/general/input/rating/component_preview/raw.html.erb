<%= render BetterUi::General::Input::Rating::Component.new(
  name: name,
  value: value,
  max_stars: max_stars,
  readonly: readonly,
  half_stars: half_stars,
  theme: theme,
  size: size,
  show_value: show_value,
  classes: classes
) %>
