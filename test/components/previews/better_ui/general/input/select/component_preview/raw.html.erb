<%= render BetterUi::General::Input::Select::Component.new(
  name: name,
  options: options,
  placeholder: placeholder,
  required: required,
  disabled: disabled,
  multiple: multiple,
  theme: theme,
  size: size,
  rounded: rounded,
  classes: classes
) %>