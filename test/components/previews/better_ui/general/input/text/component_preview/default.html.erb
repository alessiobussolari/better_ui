<%= bui_input_text(
  name: name,
  value: value,
  placeholder: placeholder,
  required: required,
  disabled: disabled,
  type: type,
  theme: theme,
  size: size,
  rounded: rounded,
  classes: classes
) %>
