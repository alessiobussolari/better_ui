<%= render BetterUi::General::Input::Text::Component.new(
  name: name,
  value: value,
  placeholder: placeholder,
  required: required,
  disabled: disabled,
  theme: theme,
  size: size,
  rounded: rounded,
  classes: classes
) %>
