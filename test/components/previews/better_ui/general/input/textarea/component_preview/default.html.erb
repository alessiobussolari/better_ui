<%= bui_input_textarea(
  name: name,
  value: value,
  placeholder: placeholder,
  required: required,
  disabled: disabled,
  rows: rows,
  theme: theme,
  size: size,
  rounded: rounded,
  classes: classes
) %>
