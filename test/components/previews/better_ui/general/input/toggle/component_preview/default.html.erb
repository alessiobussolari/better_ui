<%= bui_input_toggle(
  name: name,
  value: value,
  label: label,
  label_position: label_position,
  theme: theme,
  size: size,
  checked: checked,
  required: required,
  disabled: disabled,
  classes: classes
) %>
