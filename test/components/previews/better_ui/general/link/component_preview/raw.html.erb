<%= render BetterUi::General::Link::Component.new(
  label: label,
  href: href,
  theme: theme,
  orientation: orientation,
  style: style,
  size: size,
  icon: icon,
  active: active,
  disabled: disabled,
  target: target
) %>
