<%= render BetterUi::General::LinkComponent.new(
  label: label,
  href: href,
  theme: theme,
  orientation: orientation,
  style: style,
  icon: icon,
  active: active,
  disabled: disabled,
  target: target
) %>
