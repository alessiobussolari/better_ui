<%= bui_modal(
  title: title,
  theme: theme,
  size: size,
  rounded: rounded,
  backdrop: backdrop,
  closable: closable,
  classes: classes
) do %>
  <p class="text-gray-700">Contenuto del modal. Questo è un esempio di testo all'interno del modal.</p>
<% end %>
