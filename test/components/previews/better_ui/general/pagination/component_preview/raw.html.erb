<%= render BetterUi::General::Pagination::Component.new(
  current_page: current_page,
  total_pages: total_pages,
  path: path,
  theme: theme,
  size: size,
  window: window,
  show_info: show_info,
  per_page: per_page,
  classes: classes
) %>
