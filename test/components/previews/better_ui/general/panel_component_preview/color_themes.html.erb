<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4 p-4">
  <% themes.each do |theme_data| %>
    <div>
      <h3 class="text-lg font-medium mb-2"><%= theme_data[:label] %></h3>
      <%= render BetterUi::General::PanelComponent.new(
        title: "Tema #{theme_data[:label]}",
        body: "Questo è un pannello con tema #{theme_data[:label].downcase}.",
        footer: "Footer del pannello",
        variant: theme_data[:theme]
      ) %>
    </div>
  <% end %>
</div> 