<%= render BetterUi::General::PanelComponent.new(
  title: title,
  body: body,
  header: header,
  footer: footer,
  theme: theme,
  orientation: orientation,
  style: style,
  padding: padding,
  radius: radius
) %>