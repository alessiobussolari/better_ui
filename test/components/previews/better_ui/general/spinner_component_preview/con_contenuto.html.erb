<div class="p-8">
  <h1 class="text-xl font-bold mb-6">Spinner con Contenuto Personalizzato</h1>
  
  <div class="flex justify-center">
    <div class="p-6 bg-slate-100 rounded-lg w-96">
      <%= render(BetterUi::General::SpinnerComponent.new(
        theme: :blue,
        size: :medium
      )) do %>
        <div class="mt-4 text-center">
          <p>Puoi inserire contenuto personalizzato all'interno dello spinner.</p>
          <p class="mt-2">Questo può essere utile per messaggi specifici o contenuti aggiuntivi.</p>
          
          <div class="mt-4">
            <%= render BetterUi::General::ButtonComponent.new(
              label: "Annulla",
              type: :white,
              size: :small
            ) %>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div> 