<%= render BetterUi::General::TableComponent.new(
  data: data,
  headers: headers,
  caption: caption,
  theme: theme,
  orientation: orientation,
  style: style,
  radius: radius,
  striped: striped,
  hoverable: hoverable,
  bordered: bordered,
  compact: compact,
  footer: footer
) %>
